#include "main.h"
#include "main.h"

/**
 * _isdigit - checks for uppercase character
 * @c: The character to print
 * Return: On success 1 or 0 on error
 */
int _isdigit(int c);
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
