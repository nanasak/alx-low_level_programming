#include "main.h"

/**
 * _isupper - it checks for upercase character
 * @c: variable text
 * Return: Always 0
 */
int _isupper(int c)
{
if (C >= 'A' && C <= 'Z')
{
return (1);
}
else
{
return (0);
}
}

