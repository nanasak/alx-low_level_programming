#include "main.h"

/**
 * _islower - checks for lowercase characters
 * @c: the character case to be checked
 *
 * Return: 1 if c is a lowercase, else 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
