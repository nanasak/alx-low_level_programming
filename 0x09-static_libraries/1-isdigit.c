#include "main.h"

/**
 * _isdigit - used to check for digits
 * @c: integer to be checked for digits
 *
 * Return: 1 if c is a digit, 0 if not
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
