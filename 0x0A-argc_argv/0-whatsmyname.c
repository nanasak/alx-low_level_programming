#include <stdio.h>
#include "main.h"
/**
 * main - prints name of this programe
 * @argc: count of arguments
 * @argv: array of the arguments
 *
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
